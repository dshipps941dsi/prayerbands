import ContactsAdmin from '../_components/ContactsAdmin'

// Standalone route kept for old links; the same screen lives under Admin → People → Contacts.
export default function Page() { return <ContactsAdmin /> }
