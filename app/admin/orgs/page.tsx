import OrgsAdmin from '../_components/OrgsAdmin'

// Standalone route kept for old links; the same screen lives under Admin → People → Churches.
export default function Page() { return <OrgsAdmin /> }
